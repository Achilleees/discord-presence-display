# Roadmap

- [ ] Terminal focus icon — switch the small image to a terminal glyph when `focusContext === 'terminal'` (asset prepared, code not wired)
- [ ] Hero screenshot/GIF at the top of the README
- [ ] Keybinding for `claudeSpinner.toggle`
- [ ] GitHub Actions CI running `npm test && npm run typecheck && npm run build` on PR
- [ ] Issue template for bug reports and language requests
- [ ] Language additions — open an issue with the VS Code `languageId`
- [ ] Per-context cycle speeds (slower while reviewing a diff, faster while coding)
- [ ] Framework detection beyond `languageId` (Next.js, Nuxt, Rails via workspace heuristics)
